# TODO: Custom VCS Project (Upp)
## Core Features

-[ ] `init` - Initialize Repository
-[ ] `status` - Show tracked/utracked/modified files
-[ ] `add <file>` - Stage Files
-[ ] `commit -m msg` - Create commits
-[ ] `log` - Show commit history
-[ ] `branch <name>` - Create a branch
-[ ] `checkout <branch>` - Change Branches

## Advanced Features (Future)
-[ ] Avanced Branching (`branch`, `checkout`, `merge <branch>`, `mergetool`)
-[ ] Remote sync (`push`, `pull`, `clone <url>`)
-[ ] Conflict resolution
-[ ] Undoing Changes (`restore <file>`, `reset <commit>`)
-[ ] Diffing (`diff`, `diff --cached`)

## Bugs
- No Bugs
